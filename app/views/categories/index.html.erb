<ul class="category-list">
  <% @categories.each do |category| %>
    <li> <%= link_to category.name , category_path(category) %> <span><%= link_to 'x' , category_path(category)	, method: :delete %></span></li>
  <% end %>
</ul>
